# Explicit 2D graphics test device. Combine with standard.conf and -vga none.
# Driver admission is limited to this VGA-compatible, boot-owning device.
[device "graphics-test"]
  driver = "virtio-vga"
  edid = "on"
  xres = "1280"
  yres = "720"
